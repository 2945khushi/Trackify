<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Trackify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
        <item name="android:statusBarColor">@color/primaryDarkColor</item>
    </style>

    <style name="Theme.Trackify" parent="Base.Theme.Trackify" />

    <!-- High Contrast Menu Theme -->
    <style name="Theme.Trackify.Toolbar" parent="ThemeOverlay.Material3.Dark.ActionBar">
        <item name="actionMenuTextColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="Theme.Trackify.Popup" parent="ThemeOverlay.Material3.Light">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Fancy Border Box for Intro -->
    <style name="IntroBox">
        <item name="android:background">@drawable/border_box</item>
        <item name="android:padding">24dp</item>
    </style>
</resources>